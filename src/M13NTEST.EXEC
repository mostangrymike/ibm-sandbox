/* M13N native DEFLATE core regression. */
address command
say 'M13N: native window, bits, fixed Huffman, and LZ77'
'GITINFA'
if rc \= 0 then exit rc
say 'M13N NATIVE FIXED DEFLATE CORE PASSED'
exit 0
