/* M13N stage 2 native bit-reader regression. */
address command
say 'M13N TEST 1: native window and LSB-first bit reader'
'GITINFA'
if rc \= 0 then exit rc
say 'M13N NATIVE BIT READER FOUNDATION PASSED'
exit 0
